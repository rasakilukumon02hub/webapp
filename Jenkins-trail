pipeline {
  agent any 
  tools {
    maven 'Maven'
  }
  stages {
    stage ('Initialize') {
      steps {
        sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
            ''' 
      }
    }
    
    stage ('Build') {
      steps {
      sh 'mvn clean package'
       }
    }
    
    stage ('Deploy') {
      steps {
        sshagent(['SSH']) {
            sh 'scp -o StrictHostKeyChecking=no target/*.war ubuntu@18.212.15.175:/tomcat/webapps/webapp.war'
}
       }
    }

}
}
